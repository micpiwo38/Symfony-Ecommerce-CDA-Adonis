<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_produits_index') }}">SYMFONY 7</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_produits_index') }}">Produits
                    </a>
                </li>
            </ul>
            <form class="d-flex">

                {% if app.user and is_granted("ROLE_USER")  %}
                    <a href="{{path('app_logout')}}" class="btn btn-danger my-2 my-sm-0 mx-3">DECONNEXION</a>
                    <a href="{{ path('app_afficher_panier') }}" class="btn btn-info">&#x1F9FA; PANIER</a>
                {% else %}
                    <a href="{{path('app_register')}}" class="btn btn-warning my-2 my-sm-0">Inscription</a>
                    <a href="{{path('app_login')}}" class="btn btn-success my-2 my-sm-0 mx-3">CONNEXION</a>
                {% endif %}
            </form>
        </div>
    </div>
</nav>
